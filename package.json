{
  "name": "students-api",
  "version": "1.0.0",
  "description": "API de estudiantes",
  "main": "api/server.js",
  "scripts": {
    "start": "node api/server.js",
    "dev": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "engines": {
    "node": "22.x"
  }
}